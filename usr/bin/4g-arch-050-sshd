#!/usr/bin/env perl
use warnings;
use strict;

&run_command ( 'ssh-keygen -t rsa -f /etc/ssh/ssh_host_rsa_key' );
&run_command ( '/usr/sbin/sshd' );

sub run_command {
    my $command = shift;
    print "=> $command\n";
    system $command;
}

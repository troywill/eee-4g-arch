#!/usr/bin/env perl
use warnings;
use strict;

my $command = 'pacman --sync iproute2';
&run_command($command);

sub run_command {
    my $command = shift;
    print "=> $command\n" && system("$command");
}
